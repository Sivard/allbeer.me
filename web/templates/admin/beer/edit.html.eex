<h2 class='text-center'>Редактировать пиво</h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_beer_path(@conn, :update, @beer) %>

<%= link "Назад", to: admin_beer_path(@conn, :index) %>
